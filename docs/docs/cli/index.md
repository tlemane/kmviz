# CLI

<!-- termynal -->
```
$ python -m kmviz --help

Usage: python -m kmviz [OPTIONS] COMMAND [ARGS]...

Options:
  -v, --verbose [debug|info|warn|error]
                                  Verbosity level  [default: info]
  -d, --log-dir <dir>             Path to log directory
  --help                          Show this message and exit.

Commands:
  api      Query kmviz REST API
  app      Start kmviz
  config   Configuration helpers
  plugin   Plugin helpers
  version  Show version and exit
```